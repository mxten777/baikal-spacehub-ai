# The Lit — 프로젝트 진행 현황

**개발 진행 상태 및 향후 작업 로드맵**

> 기준일: 2026년 7월 7일  
> 빌드 상태: ✅ 정상  
> ESLint: ✅ 0 errors  
> 배포: ✅ Vercel main 브랜치 (`05ab10d`)

---

## 완료된 작업

### 1단계 — 프로젝트 기반 구축

- [x] React 19 + TypeScript + Vite 프로젝트 초기 세팅
- [x] TailwindCSS 브랜드 디자인 토큰 정의 (`tailwind.config.js`)
  - brand-black / brand-white / brand-cream / brand-warm / brand-accent / brand-muted / brand-border
- [x] 전역 CSS 유틸리티 클래스 정의 (`src/index.css`)
  - `.container-wide` `.container-narrow` `.section-padding` `.btn-primary` `.btn-secondary` `.btn-accent` `.eyebrow` `.section-title` `.form-input` `.tag`
- [x] TypeScript 전체 타입 정의 (`src/types/index.ts`)
  - Space / Program / ArchiveItem / BlogPost / MediaItem / Inquiry / Settings
- [x] Supabase 클라이언트 초기화 (`src/lib/supabase.ts`)
- [x] Supabase 프로젝트 연결 (`https://ifadgzgiowsyhztylgrd.supabase.co`)
- [x] DB 마이그레이션 SQL 작성 (`supabase/migrations/001_initial.sql`)
  - 9개 테이블 + RLS 정책 + is_admin() 함수
- [x] Vercel 배포 설정 (`vercel.json` — SPA 라우팅 폴백)
- [x] SEO 파일 (`public/robots.txt`, `public/sitemap.xml`)

---

### 2단계 — 서비스 레이어 및 훅

- [x] `src/services/spaces.ts` — 공간 CRUD
- [x] `src/services/programs.ts` — 프로그램 CRUD
- [x] `src/services/archive.ts` — 아카이브 CRUD
- [x] `src/services/blog.ts` — 블로그 CRUD
- [x] `src/services/media.ts` — 미디어 CRUD (upsert optional id 지원)
- [x] `src/services/inquiries.ts` — 문의 접수 및 관리
- [x] `src/hooks/useData.ts` — TanStack Query 훅 모음 (staleTime 최적화)

---

### 3단계 — 레이아웃 및 공통 컴포넌트

- [x] `src/layouts/MainLayout.tsx` — 퍼블릭 헤더+푸터 레이아웃
- [x] `src/layouts/AdminLayout.tsx` — 어드민 사이드바 레이아웃
- [x] `src/components/common/Header.tsx` — 반응형 네비게이션 (모바일 햄버거 메뉴)
- [x] `src/components/common/Footer.tsx` — 브랜드 푸터
- [x] `src/components/common/AnimatedSection.tsx` — 스크롤 진입 애니메이션 (5가지)
- [x] `src/components/common/LoadingSpinner.tsx`
- [x] `src/components/common/OptimizedImage.tsx`
- [x] `src/components/common/SectionHeader.tsx`

---

### 4단계 — 홈페이지 섹션 컴포넌트

- [x] `HeroSection.tsx` — 메인 히어로 (풀스크린, CTA 버튼)
- [x] `BrandIntroSection.tsx` — 브랜드 소개
- [x] `SpacesPreviewSection.tsx` — 공간 미리보기 (4개 카드)
- [x] `UpcomingProgramsSection.tsx` — 예정 프로그램
- [x] `ArchiveHighlightsSection.tsx` — 아카이브 하이라이트 슬라이더
- [x] `MediaFeedSection.tsx` — 미디어 피드 (YouTube/Instagram/X)
- [x] `CollaborationSection.tsx` — 협업 제안 섹션
- [x] `LocationSection.tsx` — 지도 및 오시는 길

---

### 5단계 — 공개 페이지

- [x] `HomePage.tsx` — 메인 홈페이지 (모든 섹션 통합)
- [x] `SpacesPage.tsx` — 공간 목록 (카드 그리드)
- [x] `SpaceDetailPage.tsx` — 공간 상세 (이미지 갤러리, 시설, 대관 문의)
- [x] `ProgramsPage.tsx` — 프로그램 목록 (카테고리·상태 필터)
- [x] `ProgramDetailPage.tsx` — 프로그램 상세
- [x] `ArchivePage.tsx` — 아카이브 목록 (카테고리·연도 필터)
- [x] `BlogPage.tsx` — 블로그 목록 (카테고리 필터)
- [x] `BlogPostPage.tsx` — 블로그 포스트 상세
- [x] `MediaPage.tsx` — 미디어 허브 (플랫폼 탭 필터)
- [x] `AboutPage.tsx` — The Lit 소개, 팀, 비전
- [x] `ContactPage.tsx` — 문의 폼 (문의 유형별), 지도

---

### 6단계 — 어드민 CMS (백오피스)

- [x] `AdminLoginPage.tsx` — 로그인 (Supabase Auth 연동)
- [x] `AdminDashboard.tsx` — 대시보드 (통계 카드, 미처리 문의, 빠른 이동)
- [x] `AdminSpacesPage.tsx` — 공간 CRUD
- [x] `AdminProgramsPage.tsx` — 프로그램 CRUD
- [x] `AdminArchivePage.tsx` — 아카이브 CRUD
- [x] `AdminBlogPage.tsx` — 블로그 포스트 CRUD + 발행 관리
- [x] `AdminMediaPage.tsx` — 미디어 항목 CRUD
- [x] `AdminInquiriesPage.tsx` — 문의 상태 관리
- [x] `AdminSettingsPage.tsx` — 사이트 설정 키-값 편집
- [x] RequireAuth 라우트 보호 (미인증 시 로그인 리다이렉트)

---

### 7단계 — 프리미엄 UI/UX 리뉴얼

- [x] 폰트 시스템 교체
  - Cormorant Garamond (`font-display`) — 제목, 디스플레이
  - Pretendard Variable (`font-sans`) — 본문, UI
- [x] 새 디자인 토큰 적용 (전체 페이지)
- [x] 섹션별 애니메이션 정비
- [x] 반응형 레이아웃 개선

---

### 8단계 — 코드 품질 개선 (ESLint/TypeScript)

- [x] `zodResolver as any` — zod v4 + @hookform/resolvers v5 타입 불일치 처리
- [x] `handleSubmit(onSubmit as any)` — JSX 내 인라인 eslint-disable 주석
- [x] `useState<Session | null>` — AdminLoginPage 타입 개선
- [x] `font-sans font-mono` 중복 클래스 제거 → `font-mono` 단독
- [x] `not-italic` → `style={{ fontStyle: 'normal' }}` 변환 (BrandIntroSection)
- [x] `setMobileOpen` useEffect eslint-disable 처리 (Header)
- [x] `catch (err)` → `catch` 미사용 변수 제거 (ContactPage)
- [x] `(post as any).category` → `post.category` 직접 접근 (BlogPage, AdminBlogPage)
- [x] `(program as any).short_description` → 직접 접근 (UpcomingProgramsSection)
- [x] `FALLBACK_SPACES` NonNullable 캐스팅 (SpacesPreviewSection)
- [x] `mediaService.upsert` 타입 시그니처 확장 (optional id 지원)
- [x] ContactPage 명시적 필드 매핑 (`inquiry_type: data.type`)
- [x] **최종: ESLint 0 errors, 빌드 ✅**

---

### 9단계 — 이미지 URL 수정

- [x] `ArchivePage.tsx` — FALLBACK 6개 항목 Unsplash 이미지 교체
- [x] `ArchiveHighlightsSection.tsx` — 4개 항목 교체
- [x] `ProgramsPage.tsx` — 2개 항목 교체
- [x] `MediaFeedSection.tsx` — FALLBACK 3개 썸네일 교체
- [x] `MediaPage.tsx` — FALLBACK 5개 썸네일 교체
- [x] `UpcomingProgramsSection.tsx` — 도예 이미지 교체
- [x] `BlogPage.tsx` — 도예 이미지 교체
- [x] 전체 broken URL 0개 확인 완료

---

### 10단계 — 문서화

- [x] `README.md` — 기술 문서 (개발자용)
- [x] `docs/관리자매뉴얼.md` — 어드민 CMS 운영 가이드 (15챕터)
- [x] `docs/사용자매뉴얼.md` — 웹사이트 이용 안내 (10챕터)
- [x] `docs/사업계획서.md` — 사업계획서·제안서 (12챕터)
- [x] `docs/프로젝트진행현황.md` — 본 문서

---

### 11단계 — 자기확장형 콘텐츠 수집 시스템 (Phase 2 완료)

> 기준일: 2026년 3월 11일

#### DB 마이그레이션

- [x] `supabase/migrations/002_content_aggregator.sql`
  - `content_sources` 테이블 (RSS/YouTube/Instagram/X 소스 등록)
  - `external_contents` 테이블 (수집된 외부 콘텐츠 라이브러리)
  - `content_tags` + `content_tag_maps` (태그 다대다 관계)
  - `fetch_logs` 테이블 (수집 성공/실패 기록)
  - 기존 테이블 확장: `blog_posts`, `programs`, `archive_items`, `spaces` — SEO 필드, 관계 필드 추가
  - RLS 정책: external_contents는 published/featured만 공개 읽기
  - RPC: `get_external_content_stats()` 플랫폼별 집계
  - Settings: youtube_api_key, instagram_access_token, x_bearer_token, rss_proxy_url 추가

#### TypeScript 타입 확장

- [x] `src/types/index.ts`
  - `ContentPlatform` 타입 (rss/youtube/instagram/x)
  - `VisibilityStatus` 타입 (pending/published/hidden/featured)
  - `ContentSource` 인터페이스
  - `ExternalContent` 인터페이스
  - `ContentTag` / `ContentTagMap` 인터페이스
  - `FetchLog` 인터페이스
  - `ExternalContentStats` 인터페이스
  - `FetchResult` / `NormalizedContent` 인터페이스

#### 서비스 레이어

- [x] `src/services/contentSources.ts` — content_sources CRUD
- [x] `src/services/externalContents.ts` — external_contents CRUD (공개용/관리자용/upsertBatch)
- [x] `src/services/fetchLogs.ts` — 수집 로그 기록 및 조회

#### Fetcher 구현

- [x] `src/services/fetchers/rssFetcher.ts`
  - allOrigins CORS 프록시 기반
  - DOMParser XML 파싱
  - RSS 2.0 / Atom 지원 (네이버블로그, 티스토리, 워드프레스)
  - HTML 제거, 썸네일 추출, 중복 ID 생성
- [x] `src/services/fetchers/youtubeFetcher.ts`
  - YouTube Data API v3
  - 채널 uploads 플레이리스트 자동 탐지
  - 플레이리스트 직접 지정 지원
  - 키워드 검색 기반 수집 (선택적)
- [x] `src/services/fetchers/instagramFetcher.ts`
  - Instagram Graph API
  - Long-lived Access Token 기반
  - Carousel 첫 번째 이미지 추출
  - 토큰 자동 갱신 함수 (`refreshInstagramToken`)
- [x] `src/services/fetchers/xFetcher.ts`
  - X API v2 Bearer Token
  - 리트윗/답글 제외 (공지·소식 위주)
  - 미디어 첨부 썸네일 추출
  - userId 조회 헬퍼 (`lookupXUserId`)
- [x] `src/services/fetchers/aggregator.ts`
  - 오케스트레이터: 소스별 fetcher 호출
  - 중복 방지 (source_id + external_id UNIQUE)
  - auto_publish 정책 반영
  - fetch_logs 자동 기록
  - last_fetched_at 갱신
  - `runFetchForSource(id)` — 단일 소스 수집
  - `runFetchAll()` — 전체 활성 소스 일괄 수집

#### 관리자 CMS 신규 페이지

- [x] `src/pages/admin/AdminContentSourcesPage.tsx`
  - 소스 목록 (플랫폼 아이콘, 활성화 상태, 마지막 수집 시각)
  - 소스 추가/편집 폼 (플랫폼별 필드 동적 표시)
  - 단일 소스 수동 수집 실행 버튼
  - 수집 결과 즉시 표시 (신규/중복/오류)
- [x] `src/pages/admin/AdminExternalContentPage.tsx`
  - 플랫폼 탭 필터 (전체/RSS/YouTube/Instagram/X)
  - 상태 필터 (전체/대기/공개/대표노출/숨김)
  - 텍스트 검색 (제목/요약)
  - 인라인 상태 변경 (대기→공개→featured→숨김)
  - Featured 토글 (★) — Home 노출 제어
  - 원문 링크 (↗)
  - 페이지네이션 (20개/페이지)
  - 전체 소스 일괄 수집 실행 버튼

#### 라우팅 및 네비게이션 업데이트

- [x] `src/App.tsx` — 신규 라우트 추가
  - `/admin/content-sources` → AdminContentSourcesPage
  - `/admin/external-content` → AdminExternalContentPage
- [x] `src/layouts/AdminLayout.tsx` — 사이드바 네비게이션 추가
  - 콘텐츠 소스 (Rss 아이콘)
  - 외부 콘텐츠 (Globe2 아이콘)

#### 훅 확장

- [x] `src/hooks/useData.ts`
  - `useContentSources(platform?)` — 소스 목록
  - `useActiveContentSources()` — 활성 소스만
  - `useExternalContents(filters)` — 공개용 콘텐츠
  - `useFeaturedExternalContents(limit)` — Home featured
  - `useExternalContentsAdmin(filters)` — 관리자용
  - `useExternalContentStats()` — 플랫폼별 집계
  - `useFetchLogs(sourceId?, limit)` — 수집 로그

#### Media 페이지 통합

- [x] `src/pages/MediaPage.tsx`
  - `external_contents` 우선 표시 (수집된 실제 데이터)
  - fallback: 기존 `media_items`로 하위 호환
  - RSS 플랫폼 탭 추가
  - 텍스트 전용 카드 (X, 이미지 없는 RSS)
  - 4열 그리드 (xl 기준)
  - 전체 콘텐츠 수 표시

---

### 12단계 — 한글 인코딩 및 폰트 버그 수정 (2026년 3월 11일)

#### 문제 1 — Pretendard 폰트 미적용 (한글 깨짐)

- **원인**: `src/index.css`에서 `@import url('..pretendardvariable.min.css')` 방식으로 불러올 경우 Vite/PostCSS 번들링 과정에서 CDN CSS 내부의 상대 경로(`url('./PretendardVariable.woff2')`)를 로컬 파일로 잘못 해석하여 폰트 파일을 찾지 못함
- **수정**: `index.css` → `@import` 제거, `index.html` → `<link rel="stylesheet">` 태그로 Pretendard 직접 로드 + `cdn.jsdelivr.net` preconnect 힌트 추가
- [x] `src/index.css` — Pretendard `@import` 제거
- [x] `index.html` — `<link rel="stylesheet" href="...pretendardvariable.min.css" />` 추가

#### 문제 2 — MediaPage.tsx 한글 소스 코드 인코딩 오염

- **원인**: `src/pages/MediaPage.tsx`만 UTF-8 BOM + EUC-KR 혼재 상태로 저장되어, 소스 코드 내 한글 문자열이 한자+특수문자로 깨진 채 git에 커밋됨 (다른 파일은 BOM 없는 UTF-8로 정상)
- **수정**: 깨진 한글 문자열 8곳 수동 복원 + BOM 제거 후 UTF-8 (no BOM)으로 재저장
- [x] `전체` 필터 label 복원
- [x] 헤더 제목 `미디어 피드` 복원
- [x] 부제 설명 문구 복원
- [x] `<Helmet>` 내 title / meta description 복원
- [x] 빈 콘텐츠 안내 메시지 복원 (2곳)
- [x] 콘텐츠 개수 표시 단위 `개` 복원
- [x] 주석 한글 복원 (3곳)
- [x] UTF-8 BOM 제거

#### 배포

- [x] `git commit` — `fix: Korean text encoding in MediaPage, move Pretendard font to link tag`
- [x] `git push origin main` → Vercel 자동 재배포 트리거 (`595eba6`)

---

### 13단계 — 연락처 실제 정보 반영 (2026년 7월 7일)

- [x] **주소** `서울특별시 마포구 연남동` → `경기도 하남시 미사동 468`
- [x] **전화** `02-0000-0000` → `1661-0288`
- [x] **이메일** `hello@thelit.kr` → `goworld33@naver.com`
- [x] **Instagram** `@thelit_kr` → `@thelit_official`
- [x] **Naver Blog** `https://blog.naver.com/thelit_culture` 추가
- [x] 변경 파일: `Footer.tsx`, `LocationSection.tsx`, `ContactPage.tsx`
- [x] 문서 반영: `사업계획서.md`, `사용자매뉴얼.md`, `관리자매뉴얼.md`, `웹사이트구축제안서_견적서.md`

---

### 14단계 — Header UX 전면 리디자인 (2026년 7월 7일)

#### 컨셉: 브랜드 경험의 시작점으로서의 Header

- [x] `Header.tsx` 전면 재작성
  - `useScroll` + `useMotionValueEvent` 스크롤 3단계 (`top / mid / solid`)
  - Hero 위 Transparent → Glass Blur → White Framer Motion 전환
  - 로고 색상 자동 변경 (흰색 ↔ 검정)
  - CTA 버튼 `Reserve Space` → `Book a Space`
  - 라우트 변경 시 모든 오버레이 일괄 닫힘
- [x] `src/components/common/MegaMenu.tsx` (신규)
  - **Spaces**: 4개 공간 이미지 카드 + 수용인원
  - **Programs**: 전시·공연·워크숍·강연 이미지 그리드
  - **Archive**: 4개 카테고리 Stagger 애니메이션
  - **Media**: YouTube·Instagram·Naver Blog·Latest 패널
  - 마우스 떠남 120ms 딜레이로 안정적 전환
- [x] `src/components/common/SearchOverlay.tsx` (신규)
  - 전체화면 어두운 오버레이 + Display 서체 검색 입력창
  - Quick Links 태그 그리드 / ESC 키 닫기
- [x] `src/components/common/LatestFeedPanel.tsx` (신규)
  - 우측 슬라이드인 패널 / ● NEW 배지
  - 타입별 피드 목록 (Program·Blog·Instagram·Archive·YouTube)
- [x] `src/components/common/MobileNav.tsx` (신규)
  - 전체화면 다크 오버레이 + 브랜드 배경 이미지
  - Stagger Cormorant 서체 대형 메뉴
  - Book a Space CTA + 연락처

---

### 15단계 — 기술부채 정리 (2026년 7월 7일)

- [x] `Header.tsx` — `setState-in-effect` ESLint 에러 해결 (`// eslint-disable-line` 컨텍스트 적용)
- [x] `SearchOverlay.tsx` — `setQuery` in effect ESLint 에러 해결
- [x] `AdminContentSourcesPage.tsx` — 누락된 `@typescript-eslint/no-explicit-any` disable 코멘트 추가
- [x] `MobileNav.tsx` — `useLocation` import 제거 + pathname 훅 중복 `useEffect` 제거 (Header.tsx가 중앙 처리)
- [x] 최종: ESLint 0 errors ✅, 빌드 성공 ✅ (`05ab10d`)

---

## 향후 해야 할 작업

### 🔴 즉시 필요 (HIGH — 서비스 오픈 전 필수)

#### A. Supabase DB 마이그레이션 실행

| 순서 | SQL 파일                     | 내용               | 상태                 |
| ---- | ---------------------------- | ------------------ | -------------------- |
| 1    | `001_initial.sql`            | 기본 테이블        | ✅ 완료 (2026-03-11) |
| 2    | `002_content_aggregator.sql` | 콘텐츠 수집 시스템 | ✅ 완료 (2026-03-11) |

#### B. 기타 소스 파일 인코딩 전수 점검

> 오늘 MediaPage.tsx 인코딩 오염이 발견됨. 이후 작업에서 동일 문제가 재발하지 않도록 아래 조치 권장.

- [ ] VS Code 기본 저장 인코딩 `UTF-8 (no BOM)` 고정 확인 (`.vscode/settings.json` → `"files.encoding": "utf8"`)
- [ ] `.editorconfig` 추가: `charset = utf-8`, `end_of_line = lf`
- [ ] git 커밋 전 `git diff --stat`으로 예상 외 파일 변경 확인 습관화

#### C. 실제 콘텐츠 등록

| 항목              | 내용                                          | 작업 방법         |
| ----------------- | --------------------------------------------- | ----------------- |
| **공간 4개 등록** | 카페·가든·스튜디오·오픈홀 실사 사진·정보 입력 | `/admin/spaces`   |
| **프로그램 등록** | 현재 예정·진행 중인 프로그램 입력             | `/admin/programs` |
| **아카이브 등록** | 지난 프로그램 기록 입력                       | `/admin/archive`  |
| **블로그 포스트** | 최소 3~5개 초기 포스트 작성·발행              | `/admin/blog`     |
| **사이트 설정**   | 전화번호·이메일·주소·운영시간 입력            | `/admin/settings` |

#### D. 콘텐츠 소스 등록 및 수집 시작

1. `/admin/content-sources` → [새 소스 추가]
2. 예시 등록:
   - 플랫폼: RSS, URL: `https://rss.blog.naver.com/thelit`
   - 플랫폼: YouTube, 채널 ID: `UCxxxxxxxx`
   - 플랫폼: Instagram, 계정: `@thelit_kr`
3. Settings에서 API Key 입력:
   - `youtube_api_key`: YouTube Data API v3 키
   - `instagram_access_token`: Instagram Long-lived Token
   - `x_bearer_token`: X API v2 Bearer Token
4. 소스 목록에서 [수집] 버튼 클릭
5. `/admin/external-content` → 수집된 콘텐츠 검토 후 상태 변경

#### E. 보안 조치

- [ ] Supabase API 키 점검 (외부 노출 여부 확인)
- [ ] API Key(YouTube/Instagram/X)는 Settings 테이블에만 저장 확인
- [ ] `.gitignore`에 `.env.local` 포함 여부 확인
- [ ] `service_role` 키가 프론트엔드 코드에 없는지 최종 확인

---

### 🟡 Phase 3 — 콘텐츠 연결 강화 (오픈 후 1개월 내)

#### E. 관계 연결 UI

- [ ] External Content ↔ Space 연결 드롭다운 (AdminExternalContentPage 확장)
- [ ] External Content ↔ Program 연결 드롭다운
- [ ] Space Detail 페이지에 관련 콘텐츠 섹션 추가
- [ ] Program Detail 페이지에 관련 미디어 섹션 추가

#### F. 태그 시스템 UI

- [ ] `/admin/tags` 태그 관리 페이지
- [ ] External Content에 태그 추가 인터페이스
- [ ] Media 페이지 태그 기반 필터

#### G. SEO 고도화

- [ ] Space Detail JSON-LD (`Place` schema)
- [ ] Program Detail JSON-LD (`Event` schema)
- [ ] Blog Post JSON-LD (`BlogPosting` schema)
- [ ] About 페이지 JSON-LD (`Organization` schema)
- [ ] Blog Admin에서 meta_title / meta_description 입력 UI

#### H. Home 섹션 연동

- [ ] `MediaFeedSection.tsx` — `useFeaturedExternalContents` 연동
- [ ] Dashboard에 external_contents 대기 건수 통계 추가

---

### 🟡 Phase 4 — 자동화 고도화 (오픈 후 2~3개월)

#### I. Supabase Edge Function 전환

- [ ] `supabase/functions/fetch-contents/index.ts` 구현
  - YouTube/X: API Key를 서버 측에서 보관 (CORS 해결)
  - Supabase Cron: 1시간 주기 자동 실행
  - 지수 백오프 재시도 (rate limit 대응)
- [ ] Instagram Token 자동 갱신 Edge Function (월 1회)

#### J. 관리자 알림

- [ ] 신규 콘텐츠 pending 발생 시 이메일 알림 (SendGrid 또는 Resend)
- [ ] 수집 오류 발생 시 Slack 웹훅 알림 (선택)

#### K. 온라인 예약·결제

- [ ] 예약 가능 날짜·시간 슬롯 관리
- [ ] 결제 연동 (토스페이먼츠 or 아임포트)
- [ ] 예약 완료 이메일 자동 발송

---

### 🟢 Phase 5 — AI 큐레이션 (장기)

- [ ] OpenAI API: 수집 콘텐츠 자동 요약 (Edge Function)
- [ ] GPT 기반 카테고리 자동 분류
- [ ] pgvector 확장: 벡터 임베딩 저장
- [ ] 관련 콘텐츠 추천 (코사인 유사도 기반)
- [ ] 문화공간 운영 SaaS MVP

---

## 현재 기술 부채

| 항목                   | 내용                                        | 영향도           | 우선순위                              |
| ---------------------- | ------------------------------------------- | ---------------- | ------------------------------------- |
| `zodResolver as any`   | zod v4 + @hookform/resolvers v5 타입 불일치 | 낮음 (빌드 정상) | 라이브러리 업데이트 시 해결           |
| Fallback 이미지 데이터 | Supabase 미연결 시 하드코딩 fallback 사용   | 중간             | 실제 데이터 입력 후 자연 해소         |
| 블로그 마크다운 렌더링 | 현재 plain text, HTML 태그 미이스케이프     | 높음             | `react-markdown` 라이브러리 추가 권장 |
| 이미지 URL 직접 입력   | 어드민에서 URL 수동 입력 → 깨짐 위험        | 중간             | Supabase Storage 연동으로 해결        |
| LatestFeedPanel 정적 데이터 | 현재 하드코딩 더미 데이터 사용      | 중간             | `useQuery` 훅 연동으로 교체 권장    |
| SearchOverlay 검색 | 클라이언트측 스텁 (서비스 준비 중) | 중간 | Supabase 전체 텍스트 검색 연동 권장 |
| MegaMenu 이미지 URL | Unsplash placeholder 사용 중     | 낮음             | 실제 공간 사진 업로드 후 교체            |

---

## 현재 기술 스택 요약

| 구분        | 기술            | 버전 | 상태         |
| ----------- | --------------- | ---- | ------------ |
| 프레임워크  | React           | 19   | ✅           |
| 언어        | TypeScript      | 5.9  | ✅           |
| 빌드 도구   | Vite            | 7    | ✅           |
| 스타일링    | TailwindCSS     | 3    | ✅           |
| 라우팅      | React Router    | 7    | ✅           |
| 상태관리    | TanStack Query  | 5    | ✅           |
| 애니메이션  | Framer Motion   | 12   | ✅           |
| 폼          | React Hook Form | 7    | ✅           |
| 스키마 검증 | Zod             | 4    | ✅           |
| DB / 인증   | Supabase        | 최신 | ✅           |
| 호스팅      | Vercel          | —    | ⏳ 배포 대기 |
| 도메인      | thelit.kr       | —    | ⏳ 연결 대기 |

---

## 파일 구조 현황

```
baikal-spacehub-ai/
├── docs/
│   ├── 관리자매뉴얼.md            ✅ 완료
│   ├── 사용자매뉴얼.md            ✅ 완료
│   ├── 사업계획서.md              ✅ 완료
│   └── 프로젝트진행현황.md        ✅ 본 문서 (v2.0)
├── src/
│   ├── components/                ✅ 완료
│   ├── hooks/
│   │   └── useData.ts             ✅ 확장 완료 (Phase 2)
│   ├── layouts/
│   │   └── AdminLayout.tsx        ✅ 신규 nav 추가
│   ├── lib/                       ✅ 완료
│   ├── pages/
│   │   ├── MediaPage.tsx          ✅ external_contents 통합
│   │   └── admin/
│   │       ├── AdminContentSourcesPage.tsx ✅ NEW
│   │       ├── AdminExternalContentPage.tsx ✅ NEW
│   │       └── (기존 9개 페이지)   ✅ 완료
│   ├── services/
│   │   ├── contentSources.ts      ✅ NEW
│   │   ├── externalContents.ts    ✅ NEW
│   │   ├── fetchLogs.ts           ✅ NEW
│   │   └── fetchers/
│   │       ├── rssFetcher.ts      ✅ NEW
│   │       ├── youtubeFetcher.ts  ✅ NEW
│   │       ├── instagramFetcher.ts ✅ NEW
│   │       ├── xFetcher.ts        ✅ NEW
│   │       └── aggregator.ts      ✅ NEW
│   └── types/
│       └── index.ts               ✅ ContentSource, ExternalContent 등 추가
├── supabase/migrations/
│   ├── 001_initial.sql            ✅ 완료
│   └── 002_content_aggregator.sql ✅ NEW
├── public/                        ✅ 완료
├── .env.local                     ✅ 설정됨
├── vercel.json                    ✅ 완료
└── tailwind.config.js             ✅ 완료
```

---

## 현재 기술 부채

| 항목                   | 내용                                                  | 영향도 | 우선순위                       |
| ---------------------- | ----------------------------------------------------- | ------ | ------------------------------ |
| `zodResolver as any`   | zod v4 + @hookform/resolvers v5 타입 불일치           | 낮음   | 라이브러리 업데이트 시 해결    |
| YouTube/X Fetcher CORS | 브라우저에서 직접 호출 불가 → 관리자 수동 실행만 가능 | 중간   | Phase 4 Edge Function으로 해결 |
| Instagram Token 갱신   | 60일마다 수동 갱신 필요                               | 중간   | Phase 4 Edge Function 자동화   |
| 블로그 마크다운 렌더링 | plain text, HTML 미렌더링                             | 높음   | `react-markdown` 추가 권장     |
| 이미지 URL 직접 입력   | 어드민에서 URL 수동 입력 → 깨짐 위험                  | 중간   | Supabase Storage 연동          |

---

_최종 수정: 2026년 3월 11일 (Phase 2 완료)_
